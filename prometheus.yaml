global:
  scrape_interval: 5s

rule_files:
  - "/etc/prometheus/prometheus-rules.yaml"

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:8889']

alerting:
  alertmanagers:
    - static_configs:
        - targets: []
